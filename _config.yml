remote_theme: pages-themes/tactile@v0.2.0
plugins:
- jekyll-remote-theme # add this line to the plugins list if you already have one

title: Il Mio Sito da Ingegnere
email: tua@email.it
baseurl: "" 
url: "https://tuo-nomeutente.github.io"

# Questa riga è la chiave per gli URL puliti
permalink: pretty

# Opzionale: per escludere file non necessari dalla build
exclude:
 
  - .jekyll-cache
  - node_modules
